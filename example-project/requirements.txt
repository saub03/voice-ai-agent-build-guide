# ── 공통 ──
# 최소 요건(오디오 계약)만으로도 mock 모드가 끝까지 돕니다.
numpy>=1.26
jsonschema>=4.20

# ── 4단계 서버(앱) ──
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
python-multipart>=0.0.9     # /api/text-turn · /api/audio-turn 의 Form 파싱

# ── [REAL · 선택] 실물 엔진 (mock 을 먼저 완성한 뒤 연결하세요) ──
# ASR : Apple Silicon Metal 한국어 전사
#   pip install mlx-whisper
# TTS : sherpa-onnx 로컬 한국어 합성 (Supertonic)
#   pip install sherpa-onnx
# LLM : Ollama (OpenAI 호환 API) — pip install openai + brew install ollama
#   openai>=1.30
# 테스트 오디오 생성: gtts + ffmpeg(brew install ffmpeg)
#   gTTS>=2.5
